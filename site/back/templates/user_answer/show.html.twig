{% extends 'base.html.twig' %}

{% block title %}UserAnswer{% endblock %}

{% block body %}
    <h1>UserAnswer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_answer.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ user_answer.createdAt ? user_answer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ user_answer.updatedAt ? user_answer.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_answer_index') }}">back to list</a>

    <a href="{{ path('app_user_answer_edit', {'id': user_answer.id}) }}">edit</a>

    {{ include('user_answer/_delete_form.html.twig') }}
{% endblock %}
